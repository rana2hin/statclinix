% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simple_summary.R
\name{simple_summary}
\alias{simple_summary}
\title{Create a Formatted Summary Table}
\usage{
simple_summary(
  data,
  by_variable = NULL,
  add_overall = FALSE,
  title = NULL,
  subtitle = NULL,
  save_as_word = FALSE,
  filename = "summary_table.docx"
)
}
\arguments{
\item{data}{A data.frame containing the data to summarize.}

\item{by_variable}{An optional string with the name of a column to group by.
If provided, p-values will be calculated across groups.}

\item{add_overall}{A logical value. If `TRUE` and `by_variable` is used, an
"Overall" column will be added to the table. Defaults to `FALSE`.}

\item{title}{An optional string for the table's main title.}

\item{subtitle}{An optional string for the table's subtitle.}

\item{save_as_word}{A logical value. If `TRUE`, the table will be saved as a
.docx file. Defaults to `FALSE`.}

\item{filename}{A string specifying the filename for the saved Word document.
Defaults to "summary_table.docx".}
}
\value{
A `gt` table object.
}
\description{
This function uses `gtsummary` to create a publication-ready descriptive
summary table, with options for grouping by a variable, adding p-values,
and saving the output as a Word document.
}
\examples{
\dontrun{
# Load the trial dataset for examples
trial_data <- gtsummary::trial

# Example 1: Basic summary of selected columns
simple_summary(
  data = trial_data[, c("age", "grade", "response")],
  title = "Basic Patient Summary"
)

# Example 2: Summary grouped by treatment, with an "Overall" column
simple_summary(
  data = trial_data[, c("age", "grade", "trt")],
  by_variable = "trt",
  add_overall = TRUE,
  title = "Patient Characteristics by Treatment"
)

# Example 3: Grouped summary saved to a Word document
simple_summary(
  data = trial_data[, c("marker", "stage", "trt")],
  by_variable = "trt",
  save_as_word = TRUE,
  filename = "treatment_marker_summary.docx"
)
}
}
